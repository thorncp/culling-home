<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      <%= link_to "Culling Home", root_path, class: "navbar-brand" %>
    </div>

    <div id="navbar" class="collapse navbar-collapse">

      <% if signed_in? %>
        <%= form_tag sign_out_path, method: :delete, class: "navbar-form navbar-right" do %>
          <div class="form-group">
            <%= current_user.email %>
          </div>
          <button type="submit" class="btn">Sign out</button>
        <% end %>

        <ul class="nav navbar-nav">
          <%= nav_link_to "Listings", listings_path %>
          <%= nav_link_to "Searches", searches_path %>
          <%= nav_link_to "Settings", settings_path %>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
