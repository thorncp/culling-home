<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <% if content_for? :title %>
    <title>Culling Home ・ <%= yield :title %></title>
  <% else %>
    <title>Culling Home</title>
  <% end %>

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= render "layouts/menu" %>

  <div class="container">
    <div id="alerts">
      <% flash.each do |id, message| %>
        <div class="alert alert-dismissible <%= alert_class_for id %>" role="alert">
          <button type="button" class="close" data-dismiss="alert">
            <%= icon "remove" %>
          </button>
          <%= message %>
        </div>
      <% end %>
    </div>

    <%= yield %>
  </div>

</body>
</html>
